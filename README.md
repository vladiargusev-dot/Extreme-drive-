# HTML / JavaScript Project

A starter workspace for a simple static web project.

## Files

- `index.html` — entry point for the web app
- `css/styles.css` — basic styling
- `js/script.js` — simple interactive behavior

## Usage

Open `index.html` in a browser to preview the project. Alternatively, use a live-reload extension or simple static server for local development.
